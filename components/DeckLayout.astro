---
import DeckHead from "./DeckHead.astro";
import "../theme/base.css";
import "virtual:astromotion/theme";

interface Props {
  title: string;
  description?: string;
  image?: string;
}

const { title, description, image } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <DeckHead title={title} description={description} image={image} />
  </head>
  <body>
    <slot />
  </body>
</html>
